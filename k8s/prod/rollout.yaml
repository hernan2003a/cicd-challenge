apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: challenge-app
  namespace: prod
spec:
  replicas: 2
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: challenge-app
  template:
    metadata:
      labels:
        app: challenge-app
    spec:
      containers:
      - name: app
        image: registry.infra-tools.svc.cluster.local:5000/cicd-challenge:dcc961f2fccb8ac712a9a194cbf59c525f48bd40
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3001
        env:
        - name: APP_ENV
          value: "production"
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "250m"
            memory: "256Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 3001
          initialDelaySeconds: 5
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3001
          initialDelaySeconds: 5
          periodSeconds: 10
  strategy:
    blueGreen:
      # activeService specifies the service to update with the new template hash at time of promotion
      activeService: cicd-challenge
      # previewService specifies the service to update with the new template hash before promotion
      previewService: cicd-challenge-preview
      # prePromotionAnalysis validates the preview stack before live traffic is switched.
      prePromotionAnalysis:
        templates:
          - templateName: cicd-challenge-health-check
            clusterScope: true
        args:
          - name: service-url
            value: http://cicd-challenge-preview.prod.svc.cluster.local:8080/health
      # postPromotionAnalysis validates the active service and automatically aborts/rolls back on failure.
      postPromotionAnalysis:
        templates:
          - templateName: cicd-challenge-health-check
            clusterScope: true
        args:
          - name: service-url
            value: http://cicd-challenge.prod.svc.cluster.local:8082/health
      autoPromotionEnabled: true
      scaleDownDelaySeconds: 60
---
apiVersion: v1
kind: Service
metadata:
  name: cicd-challenge
  namespace: prod
spec:
  selector:
    app: challenge-app
  ports:
    - protocol: TCP
      port: 8082
      targetPort: 3001
  type: LoadBalancer
---
apiVersion: v1
kind: Service
metadata:
  name: cicd-challenge-preview
  namespace: prod
spec:
  selector:
    app: challenge-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 3001
  type: ClusterIP

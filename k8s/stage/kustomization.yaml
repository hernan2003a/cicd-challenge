apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Stage environment manifests.
# ArgoCD syncs this path from the `stage` branch.
# The `gitops_update_stage` CI job updates stage-rollout.yaml on that branch.
resources:
  - stage-rollout.yaml
